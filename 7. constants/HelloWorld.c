#include <stdio.h>

int main() {
    // constant = fixed value that cannot be changed by the program during normal execution

    const float PI = 3.14159;

    printf("The value of pi is %f\n", PI);

    return 0;
}